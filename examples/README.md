# Examples

this directory contains examples used for testing the sandbox,
all these examples are written [fasm](https://flatassembler.net/) for low level control as the sandbox works on the instruction layer.


## Compiling

the examples can be compiled using fasm with the following command:
```
$ fasm [path to example]
```


